pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh 'touch /tmp/jenkins_test.txt'
                echo "this is first test 20230503"
            }
        }
    }
}
